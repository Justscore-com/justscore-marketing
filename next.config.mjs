/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          // Your existing patterns here...
        
        ],
      },
      
};



export default nextConfig;
